-  name: expense setup
   hosts: all
   vars:
     # Map Vault KV V2 fields to Ansible connection variables
     ansible_user: "{{ data.data.ansible_username }}"
     ansible_password: "{{ data.data.ansible_password }}"
   become: yes
   roles:
     - "{{role_name}}"

#Print_Task_Heading "Connect Mysql server"
#echo 'show databases' | mysql -h mysql-dev.awsdevops.sbs -uroot -p${mysql_password} &>>$Log
#if [ $? -ne 0 ]; then
#mysql_secure_installation --set-root-pass ${mysql_password} &>>$Log
#fi
#check_status $?